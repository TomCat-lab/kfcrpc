server:
  port: 8083

app:
  id: p1
  env: dev
  namespace: public

provider:
   metas:
     dc: bj
     gray: false
     unit: 8081
kfcrpc:
  zkRoot: kfcrpc
  zkServer: localhost:2181

logging:
  level:
    root: info
    com.cola.kfcrpc: debug